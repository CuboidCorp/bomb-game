/// <summary>
/// Enum qui fait la liste de tous les etats possibles de l'ordinateur accessible depuis la barre des tâches
/// </summary>
public enum ComputerLocation
{
    HOME,
    MANUAL,
    CALCULATOR
}
